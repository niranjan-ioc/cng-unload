# Copy to .env (never commit .env).

MONGODB_URI=mongodb+srv://USER:PASSWORD@niranjan-personal.4epozw0.mongodb.net/cng_unload?retryWrites=true&w=majority&appName=niranjan-personal

PORT=4000

# Long random string (16+ chars). Used to sign admin sessions.
ADMIN_JWT_SECRET=change-me-to-a-long-random-secret

# Admin email and password for the settings page.
ADMIN_EMAIL=niranjan.19950806@gmail.com
ADMIN_PASSWORD=sayhello

# Production: public URL of your API (no trailing slash). Dev: leave unset (Vite proxies /api).
# VITE_API_URL=https://your-api.example.com

# --- GitHub Actions (add as repository Secrets; never commit real values) ---
# NETLIFY_AUTH_TOKEN  — from Netlify → User settings → Personal access tokens
# NETLIFY_SITE_ID     — Site / Project ID from Netlify (see netlify.toml header)
# VITE_API_URL        — same as above; required at CI build time for the SPA
